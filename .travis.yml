language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-md' ]; then cd .. && eval "mv $currentfolder generator-angular-md" && cd generator-angular-md; fi
